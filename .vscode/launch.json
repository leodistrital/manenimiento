{
    "configurations": [
        {
            "type": "pwa-chrome",
            "name": "ddebugger chrome",
            "request": "launch",
            "url": "http://localhost:9000/",
            "webRoot": "${workspaceFolder}"
        }
    ]
}